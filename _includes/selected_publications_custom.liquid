{%- comment -%}
====================================================
SELECTED PUBLICATIONS — SINGLE IMAGE VERSION
모든 레이아웃 / 간격 / 크기 여기서 제어
====================================================
{%- endcomment -%}

{%- assign IMG_WIDTH = "380px" -%}
{%- assign IMG_RATIO = "4 / 3" -%}
{%- assign GAP = "2.5rem" -%}
{%- assign ITEM_MARGIN = "4.0rem" -%}


{% for block in site.data.selected_publications %}

<h3 style="
  margin-top:3.2rem;
  margin-bottom:1.4rem;
  font-size:1.9rem;
  font-weight:500;
">
  {{ block.year }}
</h3>

{% for pub in block.items %}
<div style="
  display:grid;
  grid-template-columns: {{ IMG_WIDTH }} 1fr;
  column-gap: {{ GAP }};
  margin-bottom: {{ ITEM_MARGIN }};
  align-items:start;
">

  <!-- LEFT: single image (NO CROP, AUTO SCALE) -->
  <div style="
    width:100%;
    border-radius:10px;
    overflow:hidden;
    background:#f2f2f2;
  ">
    <img
      src="{{ '/assets/img/publications/' | append: pub.images[0] | relative_url }}"
      style="
        max-width:100%;
        height:auto;
        display:block;
      "
    >
  </div>

  <!-- RIGHT: info -->
  <div>
    <div style="
      font-size:1.28rem;
      font-weight:500;
      margin-bottom:0.8rem;
      line-height:1.25;
    ">
      {{ pub.title }}
    </div>

    <div style="
      font-size:1.04rem;
      margin-bottom:0.8rem;
    ">
      {{ pub.authors }}
    </div>

    <div style="
      font-size:1.0rem;
      color:#222;   /* italic 제거 + 색 조금 진하게 */
      margin-bottom:0.8rem;
    ">
      {{ pub.venue }}
    </div>

    {% if pub.links.ieee %}
      <a href="{{ pub.links.ieee }}">[IEEE]</a>
    {% endif %}
    {% if pub.links.publisher %}
      &nbsp;<a href="{{ pub.links.publisher }}">[Publisher]</a>
    {% endif %}
  </div>

</div>
{% endfor %}
{% endfor %}
