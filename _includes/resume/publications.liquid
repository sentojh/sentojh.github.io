<style>
  /* 저자 줄 기본 스타일 */
  .pub-authors {
      font-size: 0.95rem;
      font-weight: 400;
      margin: 0 0 .25rem; }
  /* 내 이름만 밑줄 + 굵게 */
  .pub-authors .me{
    border-bottom: 2px solid currentColor;   /* 밑줄을 text-decoration 대신 border로 */
    padding-bottom: 1px;
    font-weight: 700;                         /* 굵게 */
  }
</style>

<ul class="card-text font-weight-light list-group list-group-flush">
  {%- assign publications = data[1] | sort: 'releaseDate' | reverse -%}
  {%- for content in publications -%}
    <li class="list-group-item">
      <div class="row">
        <!-- Date badge column -->
        <div class="col-2 col-sm-2 col-md-2 text-center date-column">
          {%- if content.releaseDate -%}
            {%- assign date = content.releaseDate | split: '-' | join: '.' -%}
          {%- else -%}
            {%- assign date = '' -%}
          {%- endif -%}
          <table class="table-cv">
            <tbody>
              <tr>
                <td>
                  <span class="badge font-weight-bold danger-color-dark text-uppercase align-middle" style="min-width: 75px">{{ date }}</span>
                </td>
              </tr>
            </tbody>
          </table>
        </div>

        <!-- Content column -->
        <div class="col-10 col-sm-10 col-md-10 mt-2 mt-md-0">
          <!-- Title -->
          <h6 class="title font-weight-bold ml-1 ml-md-4">
            {%- if content.url -%}
              <a href="{{ content.url }}">{{ content.name }}</a>
            {%- else -%}
              {{ content.name }}
            {%- endif -%}
          </h6>

          <!-- Authors (underline only my name) -->
          {%- assign my_name_aliases = "Jonghyeok Kim|Kim, Jonghyeok|Jonghyeok H. Kim|J. H. Kim" | split:"|" -%}
          {%- if content.authors -%}
            {%- comment -%} authors: string "A, B, C" 또는 array ["A","B","C"] 모두 지원 {%- endcomment -%}
            {%- if content.authors.first -%}
              {%- assign _authors_raw = content.authors -%}
            {%- else -%}
              {%- assign _authors_raw = content.authors | split: "," -%}  {# 콤마 기준 split (공백 유무 모두 허용) #}
            {%- endif -%}

            {%- capture authors_html -%}
              {%- for a in _authors_raw -%}
                {%- assign a_trim = a | strip -%}
                {%- assign is_me = false -%}
                {%- for alias in my_name_aliases -%}
                  {%- if a_trim == alias -%}              {# ← contains 제거: '정확히' 일치할 때만 #}
                    {%- assign is_me = true -%}
                  {%- endif -%}
                {%- endfor -%}
                {%- if is_me -%}
                  <span class="me">{{ a_trim }}</span>
                {%- else -%}
                  {{ a_trim }}
                {%- endif -%}
                {%- unless forloop.last -%}, {% endunless -%}
              {%- endfor -%}
            {%- endcapture -%}
            <p class="ml-1 ml-md-4 pub-authors">{{ authors_html | strip_newlines }}</p>
          {%- endif -%}

          <!-- Venue / publisher -->
          {%- if content.publisher -%}
            <h6 class="ml-1 ml-md-4" style="font-size: 0.95rem">{{ content.publisher }}</h6>
          {%- endif -%}

          <!-- Optional summary -->
          {%- if content.summary -%}
            <h6 class="ml-1 ml-md-4" style="font-size: 0.95rem; font-style: italic">{{ content.summary }}</h6>
          {%- endif -%}
        </div>
      </div>
    </li>
  {%- endfor -%}
</ul>